services:
  ytconverter:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ytconverter
    env_file:
      - .env
    ports:
      - "${PORT:-5000}:${PORT:-5000}"
    restart: unless-stopped
    volumes:
      - ./temp:/app/temp
      - ./cookies.txt:/app/cookies.txt:ro
